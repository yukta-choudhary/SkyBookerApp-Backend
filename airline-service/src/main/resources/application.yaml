server:
  port: 8082

spring:
  application:
    name: airline-service

  datasource:
    url: jdbc:mysql://localhost:3306/skybooker_airline_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: yukta@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: 9f3c7a1d5e8b4c6a2f9d1b7c3e6a8d5f2c4b7a9e1d6c3f8b2a5d7e9c1f3a6b8

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html